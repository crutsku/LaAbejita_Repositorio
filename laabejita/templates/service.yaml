apiVersion: v1
kind: Service
metadata:
  name: {{ include "laabejita.fullname" . }}
  labels:
    {{- include "laabejita.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laabejita.selectorLabels" . | nindent 4 }}
